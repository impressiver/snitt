# SDD ledger — plan: docs/superpowers/plans/2026-09-03-snitt-m3a-capture-context.md

Branch: feat/m3a-capture-context, stacked on feat/m2b-automation (PR #2, unmerged) @ d894467
Spec: docs/superpowers/specs/2026-09-02-snitt-design.md (reachable, read)
Baseline: 136 tests passing.

## Pre-flight conflict scan

### Task-pair rows (tasks sharing a file or an interface)

| Pair | Shared | Finding |
|---|---|---|
| T1 → T7 | `GitContextResolver.resolve(in:runner:)` | clean |
| T2 → T3 | `.mark`, `.marked`, `StartOptions.workingDirectory` | clean |
| T2 → T7 | `workingDirectory` produced / consumed | clean |
| T3 → T4 | `Recorder.mark`, `MarkerLog` | clean, but see F3 |
| T3, T4, T5, T7, T8 | **all modify `RecordingCoordinator.swift`** | **F4** — must be strictly sequential; no two of these may run in parallel |
| T3 → T7 | both modify `Sources/snitt-cli/main.swift` | clean — different switch arms |
| T4 → T5 | both modify `Sources/SnittApp/main.swift` | clean — different call sites, but sequential per F4 |
| T5 → T7 | `startRecording` gains `suppressFocus` then `git` | clean in the written order (5 before 7) |
| T6 → T7 | `HealthSampler` produced / consumed via `SampleBufferSink` | clean after the Fix-2 correction |
| T3, T7 → `AgentRecordingControlling` | protocol grows | **F1, F2** |

### Per-task self-agreement rows

| Task | Finding |
|---|---|
| T1 | clean — the `CommandRunner` seam makes every listed case testable without a repo. |
| T2 | clean. `RoundTripTests.versionMismatchIsRefused` uses `version + 1`, so the bump to 2 does not invalidate it. |
| T3 | **F1**, **F3** |
| T4 | **F3**. The `HotkeyMonitor` id-routing defect it fixes is real and verified in source. |
| T5 | clean — the Accessibility limitation is stated in the plan AND required in the code comment. |
| T6 | clean — `rmsIsNotMeanAmplitude` exists specifically so a mean-amplitude implementation fails. |
| T7 | **F2** |
| T8 | clean. The `ConsentExplainer` copy defect it fixes is real and verified in source. |

### Rulings

Ruling: F1 — Task 3 adds `markForAgent` to `AgentRecordingControlling`, which breaks `FakeCoordinator` in `Tests/SnittAppTests/AutomationHostTests.swift`. The plan never says to update it, so the task would not compile. Task 3's dispatch must require adding the method to the fake, recording marks the way it records `startCalls`/`stopCalls` so the host's marker path stays testable. Cost if wrong: a compile error caught in the first test run.

Ruling: F2 — Task 7 writes `startForAgent(reference:git:)`. The real signature is `startForAgent(sessionID: String, reference: TargetReference) async -> CoordinatorOutcome`. Correct form is `startForAgent(sessionID:reference:git:)`, and `FakeCoordinator` must be updated again. Carried into Task 7's dispatch. Cost if wrong: a compile error, plus an implementer guessing at a signature.

Ruling: F3 — `Recorder` is an `actor`, so `recorder.mark(label:)` is actor-isolated and requires `await`. The plan writes `return .marked(recorder.mark(label: label))` in `markForAgent` and `active?.mark(label: label)` in `markCurrentRecording`, both without `await`; neither compiles. Both become `await`, which makes `markForAgent` genuinely async — fine, since the protocol requirement is already `async`. Cost if wrong: a compile error.

Ruling: F4 — `RecordingCoordinator.swift` is modified by five of the eight tasks. No two of T3, T4, T5, T7, T8 may have live implementers at the same time. Reviews may overlap with the next task's implementer only when their file sets are disjoint, which for this plan means effectively never on that file. Sequential execution it is; the wall-clock cost is accepted rather than risking merge damage to the app's core recording path. Cost if wrong: lost parallelism, no correctness risk.

Ruling: the `startRecording` signature grows across two tasks (T5 adds `suppressFocus`, T7 adds `git`). Each task's dispatch states the signature it INHERITS and the one it LEAVES, so no implementer has to infer it from a neighbouring task it cannot see.

Task 1: implemented (haiku, agent a66e843) — BASE d894467, commit a0c6728, 141/141.
Task 1: complete (commits d894467..a0c6728, review clean)
Task 1: Ruling (carried to Task 7): the reviewer's Minor is load-bearing downstream, so it is not being deferred. `CommandRunner.git` runs `/usr/bin/env git`, which resolves via PATH. The resolver's ONLY production caller will be inside Snitt.app, launched by LaunchServices with the default minimal PATH — no Homebrew, no shell profile. Verified /usr/bin/git exists and works independently of PATH, so Task 7 changes the runner to invoke /usr/bin/git directly with an env fallback. Without this the git-context feature would silently produce nothing in exactly the configuration it ships in, and every test would still pass because they inject a fake runner. Cost if wrong: a machine with no /usr/bin/git falls back to the env lookup, which is today's behaviour.
Task 2: implemented (haiku, agent a920447) — commit 6fd31b3, 146/146. Needed the temporary .mark stub in AutomationHost plus .marked arms in CLI/MCP for exhaustive switches; Task 3 replaces all three.
Task 2: complete (commits a0c6728..6fd31b3, review clean)
Task 2: minor (deferred): the .mark stub in AutomationHost carries no TODO marker in source — its temporariness is conveyed only by the message text. Genuinely inert: no CLI or MCP path constructs .mark today, and Task 3 replaces the whole switch. Resolves itself in Task 3.
Task 3: dispatched (sonnet, agent a3dadec) — BASE 6fd31b3. Carries three brief corrections (missing await on actor-isolated mark, FakeCoordinator conformance, real startForAgent signature) plus the ownership-inside-the-actor requirement.
Task 3: implemented (sonnet, agent a3dadec) — commit 97024a8, 152/152, all three Task 2 stubs gone. Reported DONE_WITH_CONCERNS.
Task 3: RULING CORRECTED — my brief's `Recorder.mark` spawned a detached `Task { await markers.add(...) }` and returned immediately, so a mark is not ordered against `stop()`'s snapshot. The implementer flagged it rather than silently deviating, which is exactly right. It is Critical, not theoretical: `record mark` immediately followed by `record stop` is THE agent sequence, and when the race loses the marker vanishes from events.json while `mark` has already returned a timestamp — an agent told the marker landed when it did not, which is the failure shape section 11 forbids. Fix: make `mark` async and await the log directly; the actor's own queue then orders it. Handled before review per the DONE_WITH_CONCERNS rule (correctness concerns are addressed first). Cost if wrong: one awaited hop that was already happening.
Task 3: fix round 1 — mark() now awaits the write (commit e5d5850), 153/153. The implementer could NOT make the pre-fix version fail across 110 attempts including a 20-way concurrent variant, and said so rather than claiming the test caught the defect. Its explanation is correct: a non-detached Task created inside an actor-isolated method inherits that actor's context, so the write was enqueued ahead of stop(); both then serialise on MarkerLog's executor in enqueue order. Neither is a guarantee — actors are reentrant and executors are not specified FIFO — so the old code worked by scheduler behaviour, not by construction.
Task 3: Ruling: accept a non-discriminating test here, honestly labelled, rather than widen production API to prove the fix. The alternative was a delay-injection seam in MarkerLog or AssetWriterSink existing solely for one test — production surface added for test convenience, which the M2b final review flagged as a thing to scrutinise. The test guards the end-to-end path; the ordering guarantee lives in `mark` being awaited, and the source comment now says exactly that. Cost if wrong: a future refactor could reintroduce fire-and-forget and this test would not catch it — which is why the comment names where the guarantee actually lives.
Task 3: RULING CORRECTED — my brief put markers on the wrong clock, confirmed by the reviewer against source. `Recorder.mark` used `Date().timeIntervalSince(startedAt)` with `startedAt` stamped in `start()` BEFORE `await session.start()` runs, while the video's t=0 is the first sample buffer's presentationTimeStamp on SCStream's host clock. Two clocks, two origins, separated by unbounded stream-startup latency — so a marker reading "12.5s" is not 12.5s into capture.mov. It matters because section 4.12's entire purpose is jump-to-marker navigation and M3b builds WebVTT chapters on these offsets; a marker that sends a reviewer confidently to the wrong moment is the same failure shape as the race already fixed. Fix: markers move to the media clock via CaptureSession.mediaOffsetNow(), with a pure `mediaOffset(from:to:)` so the arithmetic is testable without a controllable clock. Explicitly NOT changing startedAt or durationSeconds — separate quantities with separate consumers, and conflating them is how this class spreads. Cost if wrong: markers land at a slightly different offset than today, which is the point.
Task 3: fix round 2 — markers moved to the media clock (commit 8473451), 155/155.
Task 3: fix round 3 — media-clock assumption guarded + synthetic buffers host-anchored (commit 3ab630f), 158/158. Observed end-to-end marker offset went from ~1,644,292s to 0.0235s. The 1.6M figure came from the implementer reporting a NUMBER that looked wrong beside a test that was green — markJustBeforeStopSurvives only checked that a marker existed, never its value. It now asserts plausibility.
Task 3: Ruling: added a production guard (plausibleOffset) falling back to wall clock when the media offset disagrees with it by more than 5s. Justification: the harness demonstrated this arithmetic CAN yield seconds-since-boot when the clock bases differ, and no test can check SCStream's actual clock base without a live display. Slack is deliberately generous — stream startup is hundreds of ms, a clock-base mismatch is off by orders of magnitude — and that reasoning is in the comment so nobody tightens it into firing on real recordings. Cost if wrong: on a machine where the media clock legitimately differs from wall clock by >5s, markers silently degrade to wall-clock precision instead of failing.
Task 3: complete (commits 6fd31b3..3ab630f, review clean after 3 fix rounds)
Task 3: the re-review answered the question the round-2 fix left open — whether it corrected the misalignment or merely relocated it. firstPresentationTime IS the exact timestamp passed to sink.begin(at:), which AssetWriterSink hands to writer.startSession(atSourceTime:). Writer t=0 and marker origin are the identical value, so markers now genuinely index into capture.mov.
Task 3: the reviewer improved on my analysis of the 5s guard: the risk window is ONLY the startup gap between Recorder.start()'s Date() stamp and the first delivered frame, because once running, wallClock and media advance together — a mid-recording stall affects both equally and does not move their difference. So the guard cannot drift into firing on a long recording; it fires only if SCStream takes >5s to deliver a first frame, where falling back is the correct behaviour anyway.
Task 4: implemented (sonnet, agent acc0be4) — commit f6378ad, 161/161. Discriminating check confirmed the routing defect: with the guard reverted one keypress fired the callback twice.
Task 4: fix round 1/5 dispatched — 2 Important. (a) GetEventParameter's status is discarded; a failed read leaves firedID zero-initialised and is harmless ONLY because nextHotKeyID pre-increments so 0 is never issued. Correct by unstated invariant rather than construction — if the counter ever starts at 0, a failed read fires the first monitor on arbitrary events. Made explicit. (b) registrationsGetDistinctIDs tested the ALLOCATOR, not the registration, so it would have passed unchanged against the original hard-coded `id: 1` defect — the bug lived in start(), not the allocator. Fixed by extracting `registrationID` so a test can assert the registration uses the instance id. This is the "test verifies a property adjacent to the one that matters" class, caught before merge rather than after.
Task 4: fix round 1/5 (2 addressed; commit 4686c5a) — registrationUsesTheInstanceID confirmed failing against a hard-coded id.
Task 5: implemented (sonnet, agent af590e3) — commit 6f341ff, 164/164. Focus confirmed called after resolve and before Recorder construction. Correctly left Task 4's in-flight HotkeyMonitor changes uncommitted rather than sweeping them in.
Task 4: complete (commits 3ab630f..4686c5a, review clean after 1 fix round)
Task 6: dispatched (sonnet, agent acfbd1e) — BASE 6f341ff. Files disjoint from Tasks 4/5; explicitly told NOT to touch SampleBufferSink or CaptureSession, which Task 7 owns.
Task 5: complete (commits 4686c5a..6f341ff, review clean)
Task 5: the implementer strengthened displayTargetDoesNotActivate beyond my brief. I wrote it with pid: nil on the display descriptor, which a focuser checking only `pid == nil` would have passed without ever checking the target kind. It used a non-nil pid (99) so the test discriminates the property it actually claims, and documented why inline. Unprompted improvement in the right direction.
Task 5: minor (deferred): the `focus(_ target: ResolvedTarget)` convenience overload my brief specified is unused — callers use focus(descriptor:) directly. Dead API surface; final review should delete it unless a later task needs it.
Task 6: implemented (sonnet, agent acfbd1e) — commit 7a517bc, 170/170. No threshold gates anything, confirmed. Honest test note: silenceHasZeroRMS and fullScaleHasRMSOne pass identically under mean-amplitude, so only rmsIsNotMeanAmplitude separates the formulas.
Task 7: implemented (sonnet, agent a5a4154) — commit 4a8c012, 175/175. /usr/bin/git confirmed first with an env fallback. Added SnittDocument to snitt-cli's deps; I verified at the binary with otool that both frontends still link ZERO capture frameworks, and that SnittDocument imports only Foundation. The addition is genuinely safe.

## RECURRENCE CLASS (third occurrence) — a guard that cannot catch its own class

Instances: (1) M2b's access-conformance guard was defeated first by doc comments, then by string literals, then by string interpolation — each time it looked like protection while a mention satisfied it. (2) M3a Task 4's registrationsGetDistinctIDs tested the ALLOCATOR while the defect lived in start()'s hard-coded id, so it would have passed against the exact bug the task fixed. (3) Found now: ThinClientConformanceTests scans IMPORTS, but its own doc comment states the original defect was `Package.swift` DECLARING dependencies that no file imported. Re-add SnittCapture to the dependency list without importing it and the guard passes while both frontends transitively link ScreenCaptureKit again.

Ruling: fix the instance AND state the class. The guard must also scan Package.swift's dependency declarations for the thin targets, since that is where the original defect actually lived. Folded into Task 7 because it just touched those dependency lists and has the context. The class itself — "a guard verifying a property adjacent to the one that matters" — goes in the plan's postmortem so the next milestone's guards are written against the defect's actual location rather than its most visible symptom. Cost if wrong: a slightly stricter Package.swift check that a legitimate future dependency has to be added to an allowlist.
Task 7: fix round 1 (1 addressed; commit cca4526), 176/176. The contrast was demonstrated, not argued: with SnittCapture re-added to snitt-cli's deps, the NEW dependency test failed naming it while the EXISTING import scan passed — proving the gap. The implementer also caught a collision in its own manifest-extraction helper (`name: "SnittAutomation"` matches the .library product entry) before it reached review.

## INCIDENT — a reviewer ran `git stash` on a live worktree
Task 6's reviewer, acting on a git status it reported as stale/misleading, ran `git stash` and `git stash pop`, which swept up another task's in-flight uncommitted work. It disclosed this at the top of its review rather than burying it, and named the dangling commit (a319a6ee) for recovery.
ASSESSED: no data lost. HEAD is cca4526 with both of Task 7's commits intact; BundleNaming.swift, HealthSampler.swift and the /usr/bin/git change are all present at HEAD; the only modified files are Task 8's own in-flight work. The dangling commit is redundant and needs no recovery.
Ruling: reviewers must not run mutating git commands. My review dispatches say "leave the tree clean" and permit throwaway tests, which this reviewer reasonably read as licence to tidy. That wording invited it. Future review dispatches in this project must say explicitly: read-only git only — no stash, checkout, reset, clean, or commit — and if the tree looks dirty, REPORT it rather than tidying, because another task is probably mid-flight. Cost of the incident this time: zero, by luck of timing rather than by design.
Task 6: review Spec OK / NOT APPROVED — 1 Critical (use-after-free risk: the retained CMBlockBuffer owning the audio samples is never referenced again, and the raw pointer is not syntactically derived from it, so ARC may release it before the read loop), 2 Important (undefended BGRA assumption that fails SILENTLY to variance-0 on a planar format, indistinguishable from a black capture; and reading only the first mBuffers, correct only while channelCount==1). Fix round 1 dispatched.
Task 8: implemented (sonnet, agent aa29ef4) — commit 9ca1476, 179/179. Independently diagnosed the conformance-guard conflict my brief created and reached the same ScreenRecordingAccess.isGranted() fix before my message arrived. Stale ConsentExplainer copy confirmed fixed.
Task 7: complete (commits 7a517bc..cca4526, review clean after 1 fix round)
Task 7: Important (carried to a fix round): BundleNaming.sanitize permits ".", so a branch named ".hidden-wip" yields a dot-prefixed bundle Finder and ls hide by default. Inherited verbatim from my brief's reference implementation. Minor: no length cap, so a >255-byte branch fails loudly at bundle creation rather than being prevented. Confirmed safe: ".." cannot survive as a whole filename because the commit and ".snitt" suffix are appended.

## INCIDENT 2 — a second reviewer ran `git stash`
Task 7's reviewer stashed the tree to verify a clean build, then restored it and verified the restore with `git diff stash@{0}` = empty. Materially more careful than incident 1, but still a mutating git op, and it left an undroppable entry because the permission classifier denied `git stash drop`. Both reviewers were dispatched BEFORE I added the read-only-git instruction, so this is my wording lagging, not disobedience. I verified the stash byte-identical to HEAD for all three files and dropped it. No loss.
Task 8: complete (commits cca4526..9ca1476, review clean). Reviewer confirmed the already-denied copy is accurate for BOTH the S5 race and a genuine prior denial — it never asserts which applies, so it cannot mislead either user. Read-only-git instruction worked: this reviewer REPORTED the dirty file instead of tidying it.
Task 8: minor (deferred): Sources/snitt-probe/main.swift also calls both TCC functions, so "every TCC call lives in one type" is aspirational rather than enforced — the guard only requires per-file pairing, which snitt-probe satisfies. Note snitt-probe's own header says "Replaced by the real app at M2"; the final review should decide whether that target should still exist at all rather than allowlisting it.
Task 6: fix round 1 (3 addressed; commit 9fc43ec), 180/180. The discriminating check corrected the reviewer's hypothesis in the worse direction: a non-BGRA (biplanar) buffer does NOT yield the milder "variance 0" case — it reads garbage and reports meanFrameVariance 1587.0, a plausible-looking number from misread memory. That is the failure mode that survives review, so the format guard matters more than the finding claimed, not less.
Task 6: fix round 1 verified — all 3 ADDRESSED. Re-review found a NEW Important of the same recurring class, this time in the test I specified: nonBGRAFrameContributesNoSample proves only the NEGATIVE case, so a guard written against the wrong constant (32ARGB rather than 32BGRA) still passes — a biplanar buffer is not 32ARGB either. That slip would skip every real frame and report nil variance in every bundle with nothing to catch it. The existing variance tests do not cover the accept path: they call the static variance(ofLuma:) helper directly and never enter observeVideo. Fix round 2 adds the positive counterpart.
Recurrence note: this is the FOURTH instance of "a guard/test verifying a property adjacent to the one that matters" in this project. Instances so far: the access-conformance scanner defeated three ways; Task 4's allocator-vs-registration test; the thin-client import scan vs declared dependencies; and now a format guard's reject path tested without its accept path. The structural lesson each time is the same — test BOTH directions of a guard, because the one-directional test passes against a guard pointed at the wrong thing.
Task 7: fix round 2 (2 addressed; commit 607e627), 183 tests. leadingDotIsStripped and longBranchNameIsTruncated both confirmed failing against the pre-fix namer; interiorDotsSurvive passed before and after, as it should. Read-only git used throughout.
Task 6: fix round 2 (1 addressed; commit 0384b51), 184 tests. Contrast demonstrated: with the guard's constant swapped to 32ARGB, the new positive test FAILED (meanFrameVariance nil) while the negative test still PASSED — the one-directional gap, shown rather than argued.
Task 6: Ruling: folding round 2's verification into the final whole-branch review rather than dispatching a separate scoped re-review. The change is a single added test whose discriminating contrast was already demonstrated both ways, and the final review reads the same diff. Cost if wrong: one test lands with its verification done by the final reviewer instead of a dedicated one.
Task 7: complete (commits 9fc43ec..607e627, review clean after 2 fix rounds)
Task 7: minor (deferred): a branch named literally "." yields "-a1b2c3d.snitt" — a leading HYPHEN. Not hidden and not empty so no fallback fires, but a leading-hyphen filename is a papercut for shell use (rm/cp parse it as flags). Untested edge case, vanishingly rare branch name. Final review may triage.
Task 6: complete (commits 9ca1476..0384b51, 2 fix rounds; round 2's verification folded into the final review by ruling)

## ALL 8 TASKS COMPLETE — final whole-branch review next. 184 tests, clean tree.

## FINAL WHOLE-BRANCH REVIEW — not ready. 3 Critical, 5 Important, ~8 Minor.
Critical 1 CONFIRMED by me: processID is set ONLY at CaptureTarget.swift:68 (the descriptor feeding headlessAvailable/listTargets). Both resolvers that actually produce a ResolvedTarget for recording — CachedTargetResolver and PickerTargetResolver — omit it, so WindowFocuser always returns false and the result is discarded with `_ =`. Task 5 delivers NOTHING in production, and every WindowFocuserTests test passes because they hand-build descriptors carrying a pid. Structurally invisible to the suite: exactly the cross-task shape the final review exists to catch.
Critical 2 CONFIRMED by the reviewer with a throwaway test: BundleNaming uses `timestamp` only in the no-git fallback, so a git-derived name is a pure function of branch+commit. Second recording on the same commit throws alreadyExists → exit 16 internal_error, permanently, until a human moves the file. Pre-M3a this was impossible.
Critical 3 CONFIRMED by me at RecordingCoordinator.swift:253,255: my Task 8 brief put NSAlert.runModal() inside startRecording, which toggle() and startForAgent() share. An agent socket request can block on a dialog no agent can see while isTransitioning is held, disabling the kill switch; and swift test hangs on any machine without the grant. The suite is green here only because this terminal holds it — environment-dependent greenness.
Important 4: --mic is a no-op end to end (AutomationHost never reads StartOptions.microphone; Recorder gets default CaptureOptions), so health.micRMS is structurally always nil and the DoD's "three numbers" is unachievable. It also leaves PermissionOnboarding.Service.microphone — built this milestone with copy and a deep link — with no production caller.
Important 5: durationSeconds (wall clock, stamped before startCapture) overstates capture.mov's length by the stream-startup latency, while markers use the media clock. The reviewer confirmed a marker can never EXCEED durationSeconds, so no marker falls outside its recording — but the two are not consistent, and M3b's chapters would inherit the skew.
Ruling: ONE fix wave covering all three Criticals, Importants 1-5, and the cheap minors. Plumbing --mic (Important 4) is included rather than correcting the DoD downward, because PermissionOnboarding.microphone was built this milestone and is dead code without it, and because mic-off-by-default keeps section 4.10's ladder intact. Cost if wrong: M3a grows by one plumbing change that M3b would otherwise have carried.

## FINAL FIX WAVE — all 3 Critical + 5 Important + minors ADDRESSED (commits 0384b51..ffc04f6), 198 tests.
Critical 1's resolution is better than what I asked for: the new unit test does NOT discriminate the call site (the implementer restored the pre-fix construction and it still passed, and said so). What closes it is structural — CaptureTargetDescriptor.init lost its processID default, so omitting it is now a COMPILE ERROR, verified: "missing argument for parameter 'processID' in call". Compile-time enforcement beats a test someone can delete.
Re-review verified every new test by mutation and found NO instance of the adjacent-property class in this wave — the first clean pass on that count all milestone.

## Non-blocking observations carried to M3b
- TOCTOU: main.swift reads `await coordinator.isRecording` in a separate suspension from toggle(), so an agent start in that window could turn a human's start into a stop. Microscopic, pre-existing shape.
- The human pre-explain modal still occupies the MainActor, so a concurrent agent start blocks at AutomationHost.pushState until dismissed. Far narrower than Critical 3 (human hotkey, ungranted machine only) but not zero.
- No guard stops a future coordinator test from omitting `ensureAccess:` and hitting the real CGRequestScreenCaptureAccess(). A source-grep guard in the test target would close it, mirroring the one already guarding the coordinator's modals.
- CaptureOptions.maxDuration is still carried and never read by CaptureSession.
- PermissionOnboarding.Service.microphone has no production caller: --mic now raises a Microphone TCC prompt UNEXPLAINED, from an accessory app with no Dock icon. Pre-explaining on the agent path would reinstate Critical 3, so this needs a human mic toggle M3a never scoped.
- `.awaitingRelaunch` also fires when the user actively DENIES, not only when the dialog fails to appear — that press gives zero feedback. Dead end lasts one press; judged acceptable.
