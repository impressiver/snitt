# SDD ledger — plan: docs/superpowers/plans/2026-09-04-snitt-m3c-trim-and-export.md

Branch: feat/m3c-trim-and-export, stacked on feat/m3b-events-and-export (PR #4, unmerged) @ 811b617
Spec: docs/superpowers/specs/2026-09-02-snitt-design.md (reachable, read)
Baseline: 230 tests passing.

## Pre-flight conflict scan

### Task-pair rows

| Pair | Shared | Finding |
|---|---|---|
| T1 → T3, T5 | `KeptRanges.compute(duration:cuts:)` | clean |
| T2 → T5 | `trimmed(keeping:duration:)`, `autoTrimCuts(events:duration:padding:)`, `AutoTrimError` | clean |
| T3 → T4, T5 | `BuiltComposition`, `CompositionError` | clean |
| T4 → T5, T6 | `MovieExporter.exportMovie`, `ExportManifest`, `WebVTTChapters.render` | clean |
| **T5 → T6** | `.trim`/`.export` request cases and `.trimmed`/`.exported` responses | **F3 — the enum-extension boundary breaks the build** |
| T5 ↔ T6 | both touch `Sources/SnittAutomation/` | different files (Protocol vs CommandLineParser/MCPBridge) |
| T3 ↔ T4 | `SnittExport` files | different files; T4 also adds `SnittDocument/ExportManifest.swift` |

### Per-task self-agreement rows

| Task | Finding |
|---|---|
| T1 | **F1** — instructs adding a `SnittExportTests` target that already exists. |
| T2 | clean. `autoTrimRefusesEmptyLog` covers both empty and markers-only, which is the case that matters. |
| T3 | clean, but the `writeSyntheticMovie` helper needs a note — see F4. |
| T4 | **F2** — `markerBeyondDurationIsClamped` asserts almost nothing. |
| T5 | clean. Correctly flags that `AutomationHost.forTesting()` does not exist yet; verified. |
| T6 | clean — correctly flags `toolNamesAreStable` needing an update, which is the failure that would otherwise look unrelated. |

### Rulings

Ruling: F1 — `Package.swift:54` already declares `.testTarget(name: "SnittExportTests", dependencies: ["SnittExport"])`. Task 1's brief says to add it. Corrected in the dispatch: the target exists, only the test file is new. Cost if wrong: an implementer wastes a minute discovering it.

Ruling: F2 — my `markerBeyondDurationIsClamped` test is `#expect(!vtt.contains(X) || vtt.contains(Y))` followed by `#expect(!vtt.isEmpty)`. The first passes whenever the left side is false regardless of the right, and the second is trivially true of any non-empty render. It asserts essentially nothing — the seventh instance of the class this project keeps hitting, written by me into a plan that names the class in its own history. Replaced with a real decision: a marker past the recording's end is DROPPED, not clamped, because a clamped cue is zero-length and a reviewer cannot navigate to it. The test asserts the cue is absent. Cost if wrong: a marker logged after the last frame (only reachable if duration metadata disagrees with the movie) silently does not appear as a chapter.

Ruling: F3 — Task 5 adds request/response cases that Task 6 consumes, so Task 5 necessarily leaves the CLI and MCP switches non-exhaustive. This is the SAME boundary that broke M3b's build between its Tasks 5 and 6; I named the pattern then — "when one task extends an enum and a later task handles the new case, the boundary between them is always a broken build" — and did not carry it into this plan's task structure. Task 5's dispatch will instruct temporary clearly-marked stub arms up front rather than letting the implementer discover it, and Task 6's dispatch will require REPLACING rather than adding alongside. Cost if wrong: one commit in the middle of the branch does not build in isolation.

Ruling: F4 — Task 3 needs a real movie on disk. `AssetWriterSink` and `SyntheticBuffers` exist in `SnittCaptureTests`, but `SnittExportTests` depends on `SnittExport` only, and `SnittExport` is forbidden from depending on `SnittCapture` (Package.swift says so explicitly, and the thin-client guard enforces the frontends' half). So the helper must be written directly against `AVAssetWriter` in `SnittExportTests`. Duplication is correct here rather than a shared test module: the alternative is a dependency the architecture forbids. Cost if wrong: ~30 lines of test-only duplication.

Task 1: implemented (haiku, agent a2b2c9f) — BASE 811b617, commit 769a8b5, 238/238. overlappingCutsMerge confirmed failing against naive sequential subtraction.
Task 1: complete (commits 811b617..769a8b5, review clean). Reviewer probed nested, adjacent, at-duration and inverted cuts with a throwaway test: all sane. Adjacent cuts [0,5],[5,10] merge (`cut.start <= last.end`) — correct, a zero gap is not survivable time. No exact-equality float comparisons.
Task 1: minor (deferred): an inverted cut (start > end) is silently dropped by the `end > start` filter. Not a crash, but a caller bug producing one gets no signal. Worth a comment or an explicit test; the type's invariant is presumably upstream.
Task 1: Ruling (carried to Task 3): the reviewer found that near-zero-length KEPT ranges survive — a cut ending at 9.999999 on a 10s recording leaves a ~1µs segment, and only zero-length CUTS are filtered. It correctly judged this out of scope for pure arithmetic: "too small to be a real export segment" is an export-layer policy, not a property of the complement. Task 3 (CompositionBuilder) is where it bites, because inserting a 1µs time range into an AVMutableComposition produces a degenerate segment rather than an error. Task 3 must drop kept ranges shorter than one frame at its composition frame rate (1/60s) before inserting, and treat "nothing survives the filter" as everythingCut so the caller still refuses rather than exporting an empty movie. Cost if wrong: a sub-frame sliver is dropped from an export, which is invisible; the alternative is a composition segment shorter than a single frame.
Task 2: implemented (haiku, agent a82a687) — commit 6a91a8a, 246/246. Both mutations confirmed: including markers in the activity set failed autoTrimRefusesEmptyLog and markersAreNotActivity.
Task 2: complete (commits 769a8b5..6a91a8a, review clean, no findings). Reviewer confirmed non-inverted cuts are structural: head = max(0, first-padding) <= first <= last <= min(duration, last+padding) = tail, so the head/tail overlap KeptRanges would silently drop is impossible by construction. Cuts feed cleanly into Task 1's complement.
Task 2: minor (deferred): the reviewer verified the single-event and unsorted-array cases with throwaway tests and deleted them, so the checked-in suite still has no coverage for either. The `.sorted()` call in autoTrimCuts is load-bearing — events.json is written sorted, so a future reader could remove it as redundant and silently break the contract for any other caller. Worth one checked-in test; the final review should triage whether it lands here or in M3d.
Task 3: implemented (sonnet, agent a51b4f5) — commit aa4089b, 252/252. Both discriminating checks confirmed; sub-frame filter added per the Task 1 ruling with sliverKeptRangeThrows covering a ~0.5us kept range. SyntheticMovie written fresh against AVAssetWriter, documented as deliberate duplication.
Task 3: review APPROVED, spec compliance OK. Reviewer verified transform order correct (preferredTransform.concatenating(scale)), passthrough composition real, sub-frame filter correct, SyntheticMovie non-vacuous. One Important: audio-track pairing has zero shipped coverage (writeSyntheticMovie writes no audio tracks, so all 6 tests run with sourceAudio == []); reviewer proved pairing correct with a throwaway and deleted it.
Ruling: run fix round 1 rather than defer. This is the SECOND instance in M3c of "reviewer proves a property with a throwaway, deletes it, shipped suite covers nothing" (Task 2's .sorted() gap was the first). Two hits = name the class and rule on the class: from here on, a reviewer that proves a property with a throwaway must either leave a shipped test behind or the finding is raised as Important, not logged as a deferred minor. Cost if wrong: one extra fix round per milestone on properties that are already correct.
Fix round 1 also restructures compactMap index pairing into explicit (source, destination) pairs so misalignment is unrepresentable, and derives the 1/60 filter threshold from frameDuration.
Task 3: fix round 1 complete, commit 8cfdd39, 253 tests. audioTracksPairBySourceNotFlattened verified failing against BOTH flattened and reversed pairing. Pairing restructured to (source, destination) tuples built in one pass. compositionFrameDuration now the single source for the 1/60 threshold and frameDuration. Scoped re-review dispatched.
Ruling: writeSyntheticMovie's switch to requestMediaDataWhenReady + DispatchGroup introduces @Sendable-capture warnings in TEST files only. The Global Constraint reads "zero source warnings" (plan line 29), which binds Sources/ — test-file warnings do not fail the task. But the re-review must answer whether they indicate a real race, because a flaky helper defending a correctness property is worse than no test: it gets disabled and the property loses its guard silently. Cost if wrong: an intermittent CI failure in the multi-track audio test.
Task 4 dispatched (sonnet, a9992364) in parallel with the re-review — disjoint files, and only implementers may not run in parallel.
Task 3: fix round 1 RE-REVIEW CLEAN. All three findings ADDRESSED, verified independently (reviewer reproduced both failure modes in a separate worktree rather than trusting the report; live tree untouched). Task 3 COMPLETE at 8cfdd39.
@Sendable warnings verdict: benign. Each input/adaptor is touched only inside its own closure on its own dedicated serial queue; nothing is shared across queues. Sources build 0 warnings; 3 warnings confined to Tests/.
OPEN FINDING for the final fix wave (test helper only, do not dispatch now — Task 4 is editing the tree): writeSyntheticMovie's group.wait() has no timeout and group.leave() is called ONLY on the success path. Neither closure checks writer.status. If the writer goes .failed mid-stream, isReadyForMoreMediaData never goes true again, the closure is never re-invoked, leave() never runs, and the test hangs forever with no diagnostic. Fix: check writer.status == .failed in each closure and leave() there too, and/or group.wait(timeout:). Narrow (needs a mid-stream failure in a tiny in-memory fixture) but an unbounded CI hang is a bad failure mode.
Ruling: defer to the M3c fix wave rather than a second fix round. The hazard is in a test helper, needs a failure that does not occur in the fixture as written, and dispatching an implementer now would collide with Task 4's live edits. Cost if wrong: a hung CI job that must be killed by hand.
Note: re-reviewer observed Tests/SnittExportTests/WebVTTChaptersTests.swift appear untracked mid-review (Task 4's in-progress work, TDD red phase). Correctly identified as another task's concurrent edit and left alone. Tree is transiently unbuildable by design during Task 4's red phase.

Task 4: implemented 739b350 (MovieExporter, ExportManifest, WebVTTChapters, MarkerMapping). Review: spec compliance OK, quality NOT APPROVED. Confirmed both findings I raised pre-review (swallowed EventLog read; duplicated kept-range derivation). Implementer also rewrote my own plan's markerBeyondDurationIsClamped after proving it tautological against a backwards cue -- that closes instance #7 of the adjacent-property defect class, caught by the implementer rather than shipped.

ROOT CAUSE OF THE "SANDBOX HANG" -- diagnosed by me, both prior theories wrong:
swift-testing runs tests in PARALLEL by default (no .serialized, no @Suite anywhere in Tests/SnittExportTests/). writeSyntheticMovie is SYNCHRONOUS and blocks its calling thread twice (group.wait() line 152, finishSemaphore.wait() line 156). Callers are async tests running on Swift's cooperative thread pool, which is one thread per core. Enough concurrent tests block in there, the pool is exhausted, and the callbacks that would signal the group/semaphore can never be scheduled. `sample` on the child swiftpm-testing-helper caught 8+ tests simultaneously parked at SyntheticMovie.swift:156 in _dispatch_semaphore_wait_slow.
Explains every observation: per-file filters pass (fewer concurrent tests, pool never exhausted), whole suite hangs (crosses pool width).
REFUTED: implementer's "environmental/sandbox" claim, and the re-review's "mid-stream AVAssetWriter failure" hazard. The deferred group.wait(timeout:) fix would have converted a deadlock into a spurious FAILURE while leaving the cause in place -- worse than useless, because it would have looked like a fixed flake.
Ruling: fix is to make writeSyntheticMovie async with withCheckedContinuation, never blocking a thread from an async context. Supersedes the deferred timeout finding logged after Task 3's re-review -- that entry is now CLOSED as the wrong fix for a misdiagnosed cause.
Acceptance bar for this fix round: a COMPLETE `swift test` run. Nobody has demonstrated one on this branch yet; the 253 baseline came from per-file filters and is unverified as a whole-suite number.

LESSON (9th instance of the project's recurring class, new variant): the test harness had a property production doesn't -- here it was parallelism, and the harness deadlocked itself. Every prior instance was a test too WEAK to catch a bug. This is the first where the harness was actively broken and two separate agents rationalised it as external.
Task 4: fix round 1 complete, c8fcee5. All four items fixed. Implementer REPRODUCED the deadlock first (reverted to blocking impl, sampled, confirmed 8+ threads at SyntheticMovie.swift:156) before fixing -- correct method, not just taking my diagnosis on faith.
VERIFIED BY ME: full unfiltered `swift test` = 269 tests, 1 suite, passed in 3.084s, wall 5s, exit 0. This is the first verified whole-suite green run on this branch. The prior 253 figure was per-file filters and is superseded.
Task 4: fix round 1 RE-REVIEW. Findings 2, 3, 4 ADDRESSED. Finding 1 (deadlock) addressed and STABLE -- reviewer ran the full suite 3x: 269 tests, 0 failures, 3.21/3.31/3.24s. Not fixed by luck. Double-resume safe: OnceFlag uses a real NSLock, not a plain Bool, and guards both group.leave() and every continuation resume. Strict-concurrency build: 0 warnings anywhere, INCLUDING Tests/ (better than the implementer's own claim of residual test warnings).
Task 4 COMPLETE at c8fcee5.

OPEN FINDING carried to the M3c fix wave (test helper only): a never-resume path survives in writeSyntheticMovie. The per-input readiness closures have returns that do not call group.leave() -- `guard let pool = adaptor.pixelBufferPool else { return }` and the analogous sample-buffer guard -- and writer.status == .failed is checked ONLY in the finishWriting completion, never inside the video/audio loops. If the writer fails mid-stream, isReadyForMoreMediaData goes false, the closure is never re-invoked, that input's group.enter() is never matched, and group.notify's continuation never resumes. Hangs exactly like the bug just fixed.
Correct fix: check writer.status == .failed inside each per-input loop and leave() there. NOT a group.wait(timeout:) -- that finding was closed earlier as the wrong fix for a misdiagnosed cause, and this is the genuine hazard that survived the misdiagnosis. Narrow (needs a mid-stream failure in a silent synthetic fixture) but the failure mode is an unbounded CI hang with no diagnostic, which this milestone has now paid for once.
Ruling: defer rather than open fix round 2. The skill forbids parallel implementers and Task 5 is live. Cost if wrong: one hung CI job requiring a manual kill.
Task 5: implemented 9cb07f6. Protocol v2 amended a 4th time (.trim/.export/.trimmed/.exported + TrimSummary), handlers in AutomationHost running in the app process, AutomationHost.forTesting() + NullCoordinator seam added. Package.swift gained SnittAutomation as an explicit SnittAppTests dependency (previously worked incidentally via SwiftPM's shared build dir).
VERIFIED BY ME: full unfiltered swift test = 272 tests, 1 suite, 3.255s, exit 0.
Notable: implementer verified the §4.9 thin-client constraint with `otool -L` on both binaries -- linker-level evidence that neither snitt-cli nor snitt-mcp links AVFoundation/ScreenCaptureKit. That is strictly stronger than the existing import-scanning guard, which was defeated 3 times earlier in this project. Adopting otool as the standard check for this constraint from here on.
Implementer's own flagged concerns carried into review: (a) the export failure-mapping catch-all is untested against a real MovieExporter failure; (b) CLI/MCP have no user-facing subcommand yet -- correctly deferred, that is Task 6's job.
Task 6 dispatched (sonnet, a1511b58) in parallel with Task 5's review -- disjoint surfaces, and only implementers may not run in parallel.
Task 5: review APPROVED, spec compliance OK, no Critical or Important findings. Task 5 COMPLETE at 9cb07f6.
Reviewer independently confirmed in a scratch worktree: 272/272, zero strict-concurrency warnings, ThinClient filter 3/3, and otool -L on both binaries shows no AVFoundation/ScreenCaptureKit/CoreMedia/CoreAudio linkage. SnittAutomation still depends only on SnittDocument.
NullCoordinator confined: it is `private`, forTesting() follows the existing Recorder.forTesting/CaptureSession.forTesting idiom, and main.swift only ever constructs AutomationHost with a real coordinator. Not reachable in production.
Package.swift SnittAppTests -> SnittAutomation edge is a genuine fix, not scope creep: the new test does @testable import SnittAutomation and AutomationHostTests was already relying on SwiftPM's shared build dir without declaring it.
Concurrency resolved: AutomationServer gives each connection its own OS thread (Thread.detachNewThread); respond()'s DispatchSemaphore.wait() blocks only that dedicated thread, never a cooperative-pool thread. Pre-existing infra, safe. NOTE the symmetry -- this is the same blocking call that deadlocked the suite in writeSyntheticMovie; safe here purely because the thread is dedicated rather than pool-owned. Worth a look in the whole-branch review for any OTHER blocking wait that sits on a pool thread.
Minor deferred to the fix wave: the generic export catch-all's message/hint shape is untested against a real MovieExporter failure. Reviewer graded it correctly as a formatting nicety, not a correctness risk -- ExportError and the catch-all both return .failure with String(describing:) as hint, so a failure is never reported as success. §8's bar is met.
Task 6: implemented e3c47ac. CLI subcommands + MCP tools for trim/export, numericValue helper in MCPBridge to accept JSON numbers as Int/Double/NSNumber, MCPBridgeTests.everyToolMaps fixtures extended for the two new tools.
VERIFIED BY ME: full unfiltered swift test = 279 tests, 1 suite, 3.213s, exit 0.
Plan defect noted (harmless): the Task 6 brief states a 268 total, contradicting its own 272 baseline + 7 new = 279. The implementer followed the dispatch, reported the observed count, and flagged the discrepancy instead of bending its work to match a wrong number. Correct behaviour -- recorded so the final review does not re-raise it as a defect in the code.
All 6 M3c tasks implemented. Remaining: Task 6 review, whole-branch review (most capable model), fix wave for the 3 deferred items, PR #5 stacked on #4.
DEFERRED ITEMS for the fix wave, consolidated:
  1. writeSyntheticMovie never-resume: per-input readiness closures have `guard ... else { return }` paths that skip group.leave(), and writer.status == .failed is checked only in the finishWriting completion, never inside the video/audio loops. Mid-stream writer failure leaves an enter() unmatched and hangs the continuation. Fix: check writer.status == .failed inside each per-input loop and leave() there. (test helper only)
  2. Task 5's generic export catch-all message/hint shape untested against a real MovieExporter failure. (formatting, not correctness -- never reports success on failure)
  3. Task 2's autoTrimCuts .sorted() is load-bearing but uncovered; a future reader could delete it as redundant since events.json is written sorted, and every checked-in test would still pass.
Task 6: review NOT APPROVED. Spec compliance OK (all brief steps present; otool re-run clean; 279 confirmed; brief's 268 correctly identified as a stale typo, not a code defect).
CRITICAL, verified LIVE against the running MCP server rather than by reading code -- MCPBridge.numericValue silently discards malformed input and degrades to a default:
  (a) snitt_export with "scale": "0.5" (a JSON string) parses fine, scale falls back to 1.0, and a FULL-scale export is reported as success. Agent asked for half, got full, told it worked.
  (b) snitt_trim with {"start": true, "end": 9} -- numericValue rejects the bool, start becomes nil, but the guard `auto || start != nil || end != nil` still passes because end is valid, so trim(start: nil, end: 9) is sent. A different trim than requested, no error.
This is INSTANCE #10 of the adjacent-property class, and the first to land on the FRONTEND. The checked-in tests cover the empty-trim case, so the bug settled on start/end/scale instead. My dispatch named this failure ("a malformed --trim must never degrade into trim nothing") and it still shipped on the parameters I did not enumerate -- naming one instance of a class does not guard the class.
Ruling: fix round 1. numericValue must distinguish absent (legitimate, keep default) from present-but-unparseable (fail the call, naming the parameter and what it received). One rejected parameter must not pass because a sibling is valid. JSON integer 1 must still be accepted as 1.0 -- agents send `1`, not `1.0`. Reject strings, bools, NaN, infinity; note a JSON bool bridges to NSNumber and looks numeric unless checked explicitly, which is how `true` got through.
Also in the round: reject --scale 0, negative scale, and end < start at the FRONTEND (currently deferred entirely to app-side); and extend frontendsAgreeOnTrim to exercise the CLI's own body construction from main.swift rather than comparing ParsedCommand against the MCP body.
Task 6: fix round 1 complete, b6f73ba. numericValue now returns Result<Double?, MCPBridgeError> distinguishing absent from present-but-invalid; scale <= 0 and end <= start rejected in BOTH CommandLineParser and MCPBridge; CLI Double parsing rejects "nan"/"inf" strings (Swift's Double("nan") succeeds and returns NaN -- a real trap, good catch).
VERIFIED BY ME: full unfiltered swift test = 285 tests, 1 suite, 3.227s, exit 0.
Scoped re-review dispatched with instructions to verify BY EXECUTION, not by reading -- the original bug was invisible to static reading and was only found by calling the running MCP server. Key regression risk being checked: "scale": 1 as a JSON INTEGER must still succeed as 1.0. Agents send 1, not 1.0. Over-tightening numericValue to reject strings and bools could reject integers too, which would break the common path instead of the malformed one -- a worse bug than the one being fixed.
Also being checked: whether frontendsAgreeOnTrim calls shared production code or RE-IMPLEMENTS main.swift's mapping inline. If it duplicates the mapping, it tests a copy that can agree with the MCP body while main.swift diverges -- which would be NOT ADDRESSED regardless of what it asserts.
Task 6: fix round 1 RE-REVIEW -- finding 1 NOT ADDRESSED, regression introduced. Findings 2 and 3 ADDRESSED (verified by executing snitt-cli: --scale 0, --scale nan, --start 9 --end 5, --start inf all rejected with clear messages and exit 2; frontendsAgreeOnTrim calls the same one-line enum constructor main.swift:82 uses, not a re-implementation).
REGRESSION, verified by driving the real snitt-mcp binary over stdio JSON-RPC: "scale": 1 and "scale": 0 (and start/end of exactly 0 or 1) now FAIL with `scale must be a number, got 1`. scale: 2 and start: 1.5 are unaffected. The common case an agent sends is broken while unusual values work.
Root cause: `guard !(value is Bool)`. Swift's dynamic cast from Any holding an NSNumber to Bool SUCCEEDS for any NSNumber valued exactly 0 or 1, regardless of objCType -- reviewer confirmed objCType == "q", a genuine 64-bit integer, not a CFBoolean. JSONSerialization.jsonObject (the real decode path, snitt-mcp/main.swift:96) produces NSNumber, so the bool guard eats legitimate integers.
Correct fix: discriminate on the CoreFoundation type, not a Swift cast -- CFGetTypeID(value as CFTypeRef) == CFBooleanGetTypeID(), or objCType == "c". Real JSON true/false decode to the CFBoolean singletons; integers do not.

INSTANCE #11 of the harness-has-properties-production-doesn't class, and the most consequential yet: MCPBridgeTests and frontendsAgreeOnTrim build arguments as Swift dictionary LITERALS (["start": 1]), where a boxed native Swift Int does not trigger the is Bool misclassification. Only a JSON-decoded NSNumber does. The tests exercise a TYPE THE REAL CODE PATH NEVER SEES, so a green suite certified a frontend that the shipped binary rejected.
Ruling: fix round 2 must change the tests to decode arguments from real JSON text via JSONSerialization.jsonObject -- the same call main.swift:96 makes -- not Swift dictionaries. That is the only version of these tests that could have caught this. Also required: the implementer must drive the actual snitt-mcp binary over stdio and paste observed results for scale = 1, 0, 2, 0.5, "0.5", true, and absent. A green suite is no longer sufficient evidence for this file; it was green while the binary was broken.
Cost if wrong: none material -- worst case the tests get more realistic fixtures than strictly needed.
Task 6: fix round 2 RE-REVIEW CLEAN. All three ADDRESSED. Task 6 COMPLETE at 917cb7a. ALL SIX M3c TASKS COMPLETE.
VERIFIED BY ME against the real snitt-mcp binary over stdio: scale 1/2/0.5/absent pass validation and reach the client layer; scale 0 rejected by name; scale "0.5" and true rejected as not-a-number. Suite = 289 tests, 3.205s, exit 0.
isJSONBoolean (CFGetTypeID vs CFBooleanGetTypeID) applied at BOTH call sites (numericValue and displayID); no `value is Bool` remains anywhere in Sources/. Reviewer confirmed the only production entry to MCPBridge.request is snitt-mcp/main.swift's stdio loop decoding via JSONSerialization.jsonObject line 96 -- there is no other path in, so every NSNumber reaching isJSONBoolean is CFSerialization-produced.
Harness fix is TOTAL, not partial: MCPBridgeTests is the only file referencing MCPBridge, and zero dict-literal fixtures remain -- every one now goes through real JSON text via a jsonArguments helper. Mutation test reproduced in a scratch worktree: reverting to `is Bool` fails exactly 4 tests (integerScaleOfOneAcceptedFromRealJSON, integerBoundsOfZeroAndOneAcceptedFromRealJSON, displayIDOfOneAcceptedFromRealJSON, frontendsAgreeOnTrim). Reviewer agrees that is the right set.
Deferred cosmetic (do not fix): rejecting scale: true reports `scale must be a number, got 1` -- the bridged numeric value rather than `true`. Diagnosis is correct and actionable; an agent self-corrects fine. Low-priority polish.
Whole-branch review dispatched on opus. M3c base d06ca2f, 11 commits, 25 files, 3462 insertions.

WHOLE-BRANCH REVIEW (opus): SHIP-WITH-CAVEATS. 3 Important, 3 Minor, all Importants confirmed by execution. Basics verified from a FROM-SCRATCH strict-concurrency build (reviewer noted its first build was cached -- worth re-checking in future reviews): 289/289, zero warnings, otool clean on both frontends, SnittExport->SnittDocument only, SnittAutomation->SnittDocument only.
IMPORTANT 1 -- THE MILESTONE'S REAL DEFECT: trim and export compute duration on DIFFERENT CLOCKS. AutomationHost.swift:145 takes duration from meta.durationSeconds = WALL time (stoppedAt - startedAt, RecordingMetadata.swift:39). CompositionBuilder loads AVURLAsset.duration = MEDIA time. So trimmed(keeping:), KeptRanges.compute and TrimSummary.keptSeconds/cutSeconds are wall-clock, while BuiltComposition.duration, the manifest and the WebVTT cues are media-clock. Verified with a 4.0s synthetic movie and meta.durationSeconds = 4.25: `snitt trim --start 1` reported keptSeconds 3.25 while the export produced durationSeconds 3.0. The agent is told a number the file does not have, on EVERY real recording. The tail cut also lands at a media position later than the user meant.
WHY NO TEST CAUGHT IT (structural, the key lesson): TrimAndExportHostTests builds metadata-only bundles with NO capture.mov; MovieExporterTests builds movie-only bundles with NO meta.json. NO TEST ANYWHERE JOINS TRIM TO EXPORT ON ONE BUNDLE. Six per-task reviews could not see it -- the defect lives entirely in the seam between tasks, which is exactly what the whole-branch review exists to find. This vindicates running it on the most capable model.
IMPORTANT 2: MCP BOOLEAN params still swallow malformed input -- MCPBridge.swift:306 (autoTrim) and :353 (chapters) use `as? Bool ?? false`. Rounds 1-2 hardened NUMERICS only. Verified against JSONSerialization-decoded JSON: {"chapters":"true"} -> chapters false, returned .success. Agent asks for chapters, export succeeds, no .vtt written, manifest chaptersPath null -- indistinguishable from a recording with no markers. Same for autoTrim:"true" alongside start/end. (chapters: 1 correctly reads true; leniency toward numbers fine, silence toward strings not.) This is the SAME class fixed twice already, surviving in the parameters nobody enumerated -- exactly how instance #10 happened.
IMPORTANT 3: relative paths resolved in the WRONG PROCESS. snitt-cli/main.swift:81-85 forwards bundlePath/outputPath verbatim; AutomationHost.swift:206,208 resolves via URL(fileURLWithPath:) against the APP's cwd, not the client's. The protocol already carries workingDirectory and startRecording sets it (main.swift:75) -- trim, export and inspect do not. `snitt export --out demo.mp4` writes relative to wherever the GUI app was launched and returns outputPath "demo.mp4" the agent cannot resolve.
MINOR: AutomationHost.swift:175 trim catch-all maps EVERY error to .targetNotFound "Could not read a recording at that path" -- a failed write (full disk, read-only bundle) is reported as a bad path. Export's catch-all (:237) is better, preserving String(describing:) in the hint.
MINOR (instance #12): MovieExporterTests.swift:132 is titled "...of the actual output file" but asserts only width>0/height>0; CompositionBuilderTests.swift:53 asserts on renderSize, the REQUEST. Nothing checks a scaled export produces a scaled file. Reviewer ran it: scale 0.5 on 320x240 yields a 160x120 file and a 160x120 manifest, so behaviour is CORRECT, only the assertion is adjacent.
MINOR: two bounded blocking waits, both pre-existing M3b, NO ACTION -- InputEventMonitor.swift:181 (2s timeout, cooperative-pool thread via the Recorder actor) and :222 (1s, main thread). Timeout-bounded so neither can deadlock the pool, though both park a thread. AutomationServer.swift:214 is the only other, on a detachNewThread thread, correctly documented safe. No never-resume/double-resume continuation outside the logged writeSyntheticMovie case.
NO FINDING: BuiltComposition's @unchecked Sendable still holds -- new members (duration: Double, keptRanges: [TimeRange]) are Sendable value types, so unchecked-ness still rests only on the two AVFoundation objects. AVAssetExportSession.videoComposition is @NSCopying, so MovieExporter cannot mutate the shared object -- which is also what makes it safe for M4 to hand one BuiltComposition to a player and an exporter at once.
§9 ANSWER (for M4's plan): satisfied, and here is why -- videoComposition is a real configured object (renderSize, frameDuration, one instruction spanning [.zero, cursor] with a layer instruction carrying preferredTransform (x) scale), so AVPlayerItem.videoComposition = built.videoComposition works as-is. Everything export-only lives in MovieExporter, not the builder, so a preview duplicates nothing. THE ONE GAP M4 MUST RESPECT: BuiltComposition carries no AVAudioMix for the EDL's trackStates mute/gain. Export ignores them today too, so the two still agree. M4 MUST ADD THE AUDIO MIX TO BuiltComposition, NOT TO THE PREVIEW, or §9's invariant breaks at exactly that point.

FIX WAVE: all 6 findings fixed. e159827 (#1,#4 clock + trim catch-all), b412213 (#2 MCP booleans), 63d1bcb (#3 path resolution), a5df23e (#5,#6 tests).
VERIFIED BY ME: full unfiltered swift test = 296 tests, 4.033s, exit 0. otool -L clean on snitt-cli.
Adjudication: extending the boolean fix to microphone/systemAudio was IN SCOPE and correct. Finding #2 named autoTrim and chapters only because those are what the reviewer happened to exercise; the finding is the CLASS. Leaving the same bug in two sibling parameters is exactly how this milestone produced the same defect three times -- each round fixed the parameters someone thought to enumerate, and the next round found it surviving in the ones nobody listed.
Ruling: OVERRIDE the codebase convention that main.swift wiring goes untested, for resolvePath specifically. Required its extraction into SnittAutomation (beside CommandLineParser, where the CLI's other pure logic already lives and is tested) with tests for relative, absolute, ~-prefixed, empty and ..-containing paths.
Rationale: every expensive defect in this milestone lived in code no test could see. Finding #3 WAS a main.swift wiring bug; finding #1 was a seam no test joined; instance #12 was an assertion aimed one layer off. Shipping the fix for an untested-wiring bug into untested wiring repeats the pattern with the lesson already in hand. Cost if wrong: one small file moved and five cheap tests.
Follow-up complete, 0b4706e: resolvePath moved to SnittAutomation/PathResolver.swift with 6 tests in PathResolverTests.swift. SnittDocument-only dependency untouched.
THE RULING PAID FOR ITSELF IMMEDIATELY: making it testable surfaced a REAL LATENT BUG that was live in the shipped finding-#3 fix -- URL(fileURLWithPath:relativeTo:) silently drops the last path component when workingDirectory does not already exist on disk, unless the base URL is built with isDirectory: true. Invisible while the function sat in main.swift. This is direct evidence for the override: the convention that main.swift wiring goes untested was itself hiding a bug in the fix for an untested-wiring bug.
Tests verified against three wrong implementations: verbatim passthrough (the actual pre-fix bug), the missing-isDirectory variant, and a naive string-concatenation join.
Suite now 302 (296 + 6). Clean strict-concurrency build zero warnings, otool clean on both binaries.

FIX WAVE RE-REVIEW: READY TO PR. All six ADDRESSED, every one verified by EXECUTION -- reviewer reverted each fix in a scratch worktree and confirmed the corresponding test fails for the right reason.
#1 CompositionBuilder.mediaDuration(of:) is now the single source; AutomationHost.trim and .build both call it. Fixture uses movieSeconds 4.0 vs wallSeconds 4.25 -- genuinely different, so the test does not fall into the identity trap. Missing/unreadable capture.mov fails explicitly with .internalError and a distinct hint, never falls back to wall clock. Matches §8.
#3 PathResolver verified against relative, absolute, ~, empty, .., trailing slash, and a working directory absent from disk. Removing isDirectory: true makes 4/6 tests fail with exactly the silent-sibling bug -- the tests discriminate for real.
#5 Broke CompositionBuilder to stop applying scale to renderSize: test fails 160x120 expected vs 320x240 actual. #6 Removing .sorted() fails autoTrimSortsBeforeTakingBookends.
SnittAutomation still declares dependencies: ["SnittDocument"] only; the PathResolver move added no edges.
M3C COMPLETE: 16 commits, 302 tests, zero warnings, ship-ready.
