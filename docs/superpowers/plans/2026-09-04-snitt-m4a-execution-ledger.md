# SDD ledger — plan: docs/superpowers/plans/2026-09-04-snitt-m4a-preview.md

PRE-FLIGHT CONFLICT SCAN:

| Tasks | Shared surface | Produces | Consumes | Finding |
|---|---|---|---|---|
| 1 -> 3 | BuiltComposition.audioMix | Task 1 adds AVAudioMix? | Task 3 assigns it to the player item | Clean. Task 3 must not build its own. |
| 1 -> 1 | BuiltComposition layout | struct gains a field | export + preview both read | CONFLICT: stale-build SIGSEGV risk, ruling R1. |
| 2 -> 3,5 | JumpPoint / MarkerJumpPoints.compute | Task 2 | Task 3 stores, Task 5 computes | Clean, signature fixed in Task 2. |
| 2 vs export | MarkerJumpPoints vs MarkerMapping | two mappers, same rule | - | CONFLICT: probable duplication, ruling R2. |
| 3 -> 4,5 | PreviewController(built:jumpPoints:) | Task 3 | Tasks 4 and 5 construct one | Clean. |
| 4 -> 5 | EditorWindowController + openWindowCount | Task 4 | Task 5 opens on stop, tests read the count | Clean. Count is static; see R3. |
| 4 self | NSApp activation policy | tests mutate global state | - | CONFLICT: parallel-execution risk, ruling R3. |
| 5 self | agent vs human stop | - | - | Clean, but load-bearing per §5.3. |

RULING R1 (BuiltComposition layout change): Task 1 adds a field to a struct that crosses module boundaries. M3d proved this produces a SIGSEGV from a stale incremental build -- one compilation unit compiled against the old layout, another against the new, and destroying the value releases a garbage pointer. Task 1's dispatch REQUIRES `rm -rf .build` before verification, and so does Task 5's, since it is the next task to touch both modules. Cost if wrong: a phantom crash investigation, which cost about an hour in M3d.

RULING R2 (two marker mappers): MarkerJumpPoints.compute and MarkerMapping.map almost certainly implement the same rule. I am NOT pre-emptively unifying them, because SnittExport depends on SnittDocument (so unification can only move export-ward) and because the export mapper is shipped, reviewed and has its own tests. Task 2's implementer must state explicitly whether the logic is identical, and the reviewer decides. Cost if wrong: two copies of a time-mapping rule drift, and a chapter list and a scrub bar disagree about one recording -- which is the M3c seam defect in a new place.

RULING R3 (NSApp policy tests mutate global state): Task 4's tests set and assert NSApp.activationPolicy(), which is process-global, while swift-testing runs tests in parallel. This is the harness-has-properties-production-does-not class that has bitten this project repeatedly. Accept the tests as designed -- the policy behaviour is genuinely important and genuinely testable -- but if new flakiness appears around Task 4, SERIALISE that suite before suspecting the feature. Do not diagnose it as a product bug first. Cost if wrong: one flaky suite chased in the wrong direction.

Scan otherwise clean. Baseline 349 at 1d42922 (verified on merged main). Branch feat/m4-timeline-and-preview, off main after the M2b-M3d stack merged.
SPIKE S6 (docs/superpowers/spikes/S6-preview-and-audio-mix.md) answered both load-bearing questions BEFORE the plan was written: AVPlayerItem accepts CompositionBuilder's real output and reaches readyToPlay in ~0.1s, and AVAudioMix binds to the composition's own audio tracks. Also surfaced the binding constraint: AVPlayerItem is main-actor isolated and AVAudioMix is non-Sendable under Swift 6, so every preview type is @MainActor by construction.

Task 1: complete 519e3b4, 353 tests (349 + 4), zero warnings, stable across clean runs. BuiltComposition.audioMix built inside build, never mutated after, consumed by MovieExporter via session.audioMix; the @unchecked Sendable doc comment now covers all three objects.
IMPLEMENTER FOUND A DEFECT IN MY PLAN'S FIXTURE (instance #17): the synthetic movie's audio track was always SILENT LPCM, so exportAppliesTheMix -- which compares muted vs unmuted exported byte sizes -- could not have discriminated a real fix from a no-op. Muting silence changes no bytes. It added a .tone (440Hz sine) option used only by that test, leaving every other call site on the silent default. Same "fixture too simple for the feature to be observable" shape as M3d's flat-gray frames, in a new place.
IMPLEMENTER ALSO ANSWERED THE TRACK-NAMING QUESTION I ASKED, and the convention does NOT hold. I verified both halves myself:
  - EditDecisionList.fullRange() (EditDecisionList.swift:47) emits ["video", "microphone", "systemAudio"] -- three states, the FIRST OF WHICH IS NOT AN AUDIO TRACK.
  - AssetWriterSink (AssetWriterSink.swift:64) calls writer.add in order [videoInput, systemInput, micInput], so a recording's AUDIO tracks are [systemAudio, microphone].
  Positional matching therefore gives audio track 0 (systemAudio) the state named "video", gives microphone its own state BY COINCIDENCE, and drops "systemAudio" entirely. Muting system audio does nothing; muting "video" silences system audio.
Ruling: FIX NOW as Task 1b, not deferred. This is §8's confidently-wrong failure -- a user mutes a track, is told it worked, nothing changes. It is latent only because nothing writes trackStates yet, and M4b's mute UI makes it immediately reachable. I deferred a hazard twice across M3c and M3d on "narrow, test-only, needs a condition the fixture cannot produce" reasoning, and it returned as a SIGSEGV that silently truncated suite runs. A hazard whose consequence I mis-predict is not a hazard I assessed.
Task 1b design: AudioTrackOrder.canonical lives in SnittDocument because SnittExport must never depend on SnittCapture (§4.9 layering, enforced by the thin-client guard), so the exporter cannot ask the recorder what it wrote. Both sides reference the shared constant, and canonicalOrderMatchesTheRecorder fails if anyone reorders the writer.add calls.
Carried into the dispatch: Task 1's tests use "audio0"/"audio1" (CompositionBuilderTests.swift:141,142,165,202), which are not canonical names and would match nothing under the fix. They must be updated to real names WITHOUT weakening assertions and WITHOUT making the matcher accept arbitrary names -- a matcher that accepts "audio0" has learned nothing.
Deferred, noted by the implementer: exportAppliesTheMix's muted-vs-loud byte comparison inherits M3d's encoder-under-load nondeterminism. Reliable in isolation, occasionally narrow under parallel load. Watch it; if it flakes, the fix is the M3d one -- assert relative to a no-op baseline measured by mutation, not an absolute margin.

Task 1b: complete 2c7877d, 356 tests. AudioTrackOrder.canonical in SnittDocument; CompositionBuilder matches trackStates BY NAME; AssetWriterSink references the shared constant. Both prescribed mutations (positional matching, swapped canonical order) produced exactly the predicted failures. otool clean, no new dependency edge. VERIFIED BY ME: 356 twice.
Task 2: complete f9d9096, 361 tests, 5/5 new tests, all 4 required mutations caught by exactly the intended test.
THE QUESTION I ASKED IS ANSWERED DEFINITIVELY: MarkerJumpPoints.compute and MarkerMapping.map(_:keptRanges:) implement BYTE-FOR-BYTE IDENTICAL mapping arithmetic -- same cursor accumulation, same closed/half-open boundary rule (last range closed, others half-open), same drop-not-clamp for markers inside a cut, same final sort. They differ ONLY in output shape: JumpPoint (non-optional label defaulting to "Marker") vs LoggedEvent (label passed through). The implementer reported the facts and explicitly did NOT refactor out of scope, which is the right call.
RULING: UNIFY, as Task 2b. Extract the shared range-walk into SnittDocument; both call sites shape their own output. SnittExport already depends on SnittDocument so no new edge is needed.
Rationale: this is the exact defect class M3c paid for -- trim and export computed duration two different ways and disagreed on every real recording, a seam six per-task reviews could not see. M3c's whole-branch review named the fix it liked best as "when two layers must agree on a derived set, pass the set, don't pass the inputs" (BuiltComposition.keptRanges), and called the same bug "the one place this discipline was not applied". Leaving two copies here would repeat it knowingly. The risk is low: both sides have tests, and the refactor is a move rather than a rewrite.
Cost if wrong: one refactor round on code that currently works, with both test suites as the net.

Tasks 1+1b REVIEW: spec compliance OK, APPROVED with one Critical follow-up. Reviewer verified everything by execution in a pinned worktree: 356 tests across 3 clean runs (~6.3s), zero warnings, otool clean on both frontends.
 - §9's audio guarantee IS genuinely guarded: commenting out `session.audioMix = built.audioMix` makes exportAppliesTheMix fail with byte-identical output (quiet 67677 == loud 67677).
 - The .tone fixture is real and opt-in; the same mutation gives identical bytes without the mix and a genuine difference with it. No measurable suite slowdown.
 - Task 1b holds: reverting to positional matching fails both name-matching tests, while canonicalOrderMatchesTheRecorder is correctly unaffected (it guards a different thing).
 - The @unchecked Sendable invariant is TRUE, not merely claimed -- mix built inside build, never mutated after, doc comment names all three objects.
CRITICAL, found by probing the stale/duplicate-state edge I asked about and NOT previously flagged by anyone: audioMix(for:states:) used Dictionary(uniqueKeysWithValues:), which TRAPS on a repeated key. A trackStates array naming the same track twice killed the process with signal 5. edit.json is a plain file a human can hand-edit and a bad merge can duplicate.
FIXED BY ME (small, precisely diagnosed, and Task 2b was live on different files): Dictionary(_:uniquingKeysWith:) with last-wins, documented, plus duplicateTrackNameIsSurvivable.
VERIFIED BY MUTATION: the trapping form dies with `Fatal error: Duplicate values for key: 'systemAudio'` and signal 5 -- a CRASH, not a failing assertion. That is why no existing test caught it and why the reviewer had to probe for it rather than read for it. Third time this milestone family that the wrong implementation's signature is a dead process rather than a red test.
Deferred Minor: gain outside 0...1 is passed straight to setVolume without clamping or documentation. Likely fine (AVAudioMix accepts arbitrary gain) but undocumented, unlike the well-documented unmatched-name case. One line, fix wave.

Task 2b: complete e407fdf, 363 tests. Shared range-walk extracted to SnittDocument.TimeRangeMapping.trimmedTime(of:keptRanges:); both mappers reduced to output shaping. Package.swift untouched, no new dependency edge, otool clean. NO EXISTING ASSERTION TOUCHED -- the refactor moved code without changing behaviour, which was the condition I set.
Tasks 2+2b REVIEW: spec compliance OK, APPROVED, one Minor. Reviewer verified independently rather than on report: the diff is extract-and-delegate only, the boundary rule (last range closed, others half-open) and drop-not-clamp are byte-identical, 363 matches baseline exactly with a summary line present.
The check that mattered: the reviewer PERTURBED MarkerMapping.map's output by +0.5s AT ITS CALL SITE, leaving TimeRangeMapping untouched, and previewAndExportAgreeOnMarkerTime failed as intended, then reverted byte-identically. That is precisely the drift class the shared function CANNOT prevent by construction -- independent post-processing at one call site -- so the agreement test is not redundant with the refactor. I had asked for exactly this scenario rather than the easy one.
Reviewer's own verdict on my ruling, which I asked for as a view rather than agreement: unification was right. The functions were identical down to the boundary split and drop-not-clamp; leaving copies would have reproduced the trim/export duration bug this milestone guards against; the layering made it free. Its caution is the correct one -- the shared function now hides the one-call-site bug class entirely, which is why the agreement test has to exist and has to be verified by hand.
Also confirmed: Task 2's fixture has a cut BEFORE the asserted marker (not an identity-function trap), and while markerInsideACutIsDropped alone cannot distinguish drop-in-cut from drop-everything, markersShiftByPrecedingCuts asserts count == 2 and does distinguish it -- the suite discriminates as a whole even though one test does not.
MINOR CLOSED BY ME: label: "" was handled correctly but untested; only nil was covered. The event log can legitimately store an empty string, and a `label != nil` check would pass while shipping a blank, unclickable jump point. Added emptyLabelIsNamed.

Task 3: complete 2f13ab7, 368 tests, 4 new PreviewControllerTests. Package.swift gained a SnittExport dependency for SnittAppTests (not in my file list, but required to compile the test).
INSTANCE #18, a defect in MY plan text: the brief asserted `item.videoComposition === built.videoComposition`. That is EMPIRICALLY IMPOSSIBLE for any correct implementation -- AVPlayerItem's setter takes an immutable SNAPSHOT, so the getter always returns a distinct object. No implementation could ever have passed it. The implementer replaced that one line with a renderSize equality check and kept `item.asset === built.composition`, which is the identity assertion that actually does the discriminating work against a controller that rebuilds its own composition. Correct call.
IMPORTANT, and reported rather than hidden: seekIsExact and jumpSeeksToMarkerTime DO NOT DISCRIMINATE the tolerance arguments in this environment. The implementer mutated to explicit .positiveInfinity tolerances AND to a raw AVURLAsset item, and currentTime() still came back exact both times. It kept .zero/.zero (the documented-correct choice) and ANNOTATED THE TESTS HONESTLY rather than leaving them looking stronger than they are.
My diagnosis of why: the synthetic fixture is encoded such that effectively every frame is a keyframe, so seek tolerance has nothing to snap to. On a real recording with long GOPs, a tolerant seek can land seconds from a marker -- which is exactly the bug the tolerance arguments prevent. So this is the M3d noise-fixture lesson again in a third place: the FIXTURE is too simple for the property to be observable, and the test is honest but toothless.
Ruling: fix the fixture, following the .tone precedent from Task 1 -- add an opt-in keyframe-interval parameter to writeSyntheticMovie (AVVideoMaxKeyFrameIntervalKey), defaulting to current behaviour so no other test changes, and have the seek tests use a sparse-keyframe movie. Then removing the tolerance arguments actually fails. Doing this myself as a follow-up; it is a test-only change I have fully diagnosed.
Also flagged by the implementer: one intermittent segfault on an unrelated run, cleared on retry, then 3 clean. Watching -- this branch has not had a crash before now and M3d spent real time on exactly this.

SYNTHETIC-MOVIE FIXES: 0d0d852 (pool) and 12edae1 (keyframe param), committed separately.
POOL USE-AFTER-FREE -- STRUCTURALLY FIXED. writeSyntheticMovie now allocates its OWN CVPixelBufferPool instead of borrowing adaptor.pixelBufferPool, in BOTH copies. Evidence: 15 consecutive full-suite runs, all 15 printed a summary line, 0 crashes, 0 missing-summary runs, and the swiftpm-testing-helper crash-report count was 25 before and 25 after.
Worth recording why this took three attempts across three milestones. Attempt 1: a fixed sleep. Attempt 2: a longer bounded poll. Attempt 3 (M3d): a `writer.status == .failed` check at the top of the loop -- which I accepted as complete. But the check runs at line ~174 and the crash is at ~190, so it NARROWED the window rather than closing it, and the crash is rare enough that a handful of green runs meant nothing. Each theory was plausible and each treated a symptom. The structural fix removes the MECHANISM: a pool we own cannot be torn down by the writer's state machine.
MY KEYFRAME DIAGNOSIS WAS WRONG, and the implementer proved it by measurement instead of accepting it. I theorised that seekIsExact fails to discriminate because the synthetic movie is effectively all-keyframe, so tolerance has nothing to snap to. Measured directly with AVAssetReader: setting maxKeyFrameInterval to the whole clip's frame count only reduced keyframes 9 -> 6 over ~124 frames, because VideoToolbox does not honour it as a hard floor for flat low-motion content. And decisively: even WITH sparser keyframes, dropping PreviewController.seek's tolerance arguments still landed currentTime() exactly on 2.5s and 1.75s.
The real explanation: on this toolchain AVPlayer.currentTime() after a seek reports the REQUESTED TARGET, not the actually-decoded frame. So no fixture change can make these tests discriminate tolerance -- the observable itself is wrong, not the fixture. Observing it would need AVPlayerItemVideoOutput.copyPixelBuffer(forItemTime:) to inspect the decoded frame, which is disproportionate for this property.
Ruling: ACCEPT. The tolerance arguments stay (documented-correct, and they matter on real recordings where a tolerant seek lands a marker jump seconds away), the two tests stay as regression checks that seek moves the playhead at all, and the limitation is documented in code. I am NOT reverting the keyframe parameter: it is opt-in, harmless, used, and its comment records a measured negative result that stops the next person re-running the same experiment. Cost if wrong: a small unused-looking knob in a test helper.
LESSON: I asked the implementer to tell me if sparse keyframes still did not discriminate rather than to make the test look meaningful, and that is exactly what came back. Had I framed it as "make this test discriminate", the likely outcome was a test contorted around an observable that cannot show the property.

Task 4: complete ac25336, 372 tests (368 + 4), 5 consecutive clean full-suite runs, zero warnings. PlayerLayerView + EditorWindowController.
INSTANCE #19, another defect in MY plan text: the brief's tests, run verbatim as free @MainActor functions, CRASHED with signal 5 and no summary line. Cause: bare `NSApp` is an implicitly-unwrapped optional that stays NIL until something touches NSApplication.shared, and nothing in a test bundle does that automatically. Every test I wrote referencing NSApp would have crashed the bundle.
The implementer wrapped the four tests in a @Suite(.serialized) struct whose init() touches NSApplication.shared once before each body, leaving the test BODIES unchanged from the brief. That is the right shape: it fixes the harness, not the assertions. It also happens to serialise them, which independently addresses ruling R3's parallel-mutation-of-global-NSApp-state concern -- so R3 is now closed by construction rather than by watchfulness.
Both Step-5 mutations verified: tying the policy to an isShown flag instead of the open count fails closingOneOfTwoKeepsPromotion, and dropping pause() from teardown fails closingPausesPlayback.
Noted for review: the NSWindowDelegate close-button path is intentional but untested -- closing via the window's own close button, as opposed to calling close(), is the path a real user takes most often.
Pattern across M4a so far: four defects in my own plan text (the silent-audio fixture, the impossible videoComposition identity assertion, the wrong keyframe diagnosis, and now NSApp being nil in tests). All four were found by implementers running the verification steps rather than performing them, and all four were reported rather than worked around. The mutation-check requirement plus "say so plainly if it cannot discriminate" is what produces that.

Tasks 3+4 REVIEW: spec compliance OK, APPROVED. Reviewer confirmed PreviewController contains no CompositionBuilder.build, no AVVideoComposition(, no AVMutableAudioMix( -- it attaches and never builds, and item.audioMix = built.audioMix reaches the player item. Pool fix confirmed in BOTH copies with zero remaining adaptor.pixelBufferPool reads; 5 clean runs at 372, crash-report count 25 before and after. NSApp fix confirmed to change only the harness, with the test bodies verbatim from the brief; serialisation genuinely needed (four tests mutate process-global activation policy), not masking anything.
Both of my wrong plan assertions adjudicated:
 - The impossible videoComposition identity assertion: reviewer agrees AVPlayerItem's setter snapshots into a distinct object, so the renderSize substitution is right and item.asset === built.composition still carries all the discriminating weight.
 - The wrong keyframe diagnosis: reviewer agrees it was correctly falsified, and confirms AVPlayerItemVideoOutput.copyPixelBuffer(forItemTime:) IS the right observable -- it reads actually-decoded frames, independent of currentTime()'s reporting quirk -- and is worth a follow-up but not worth blocking on. Recorded for the fix wave.
FINDING WORTH ACTING ON, and sharper than the gap itself: Task 4's report justified leaving windowWillClose untested by saying it needs a live window server. THAT JUSTIFICATION IS FALSE -- the reviewer verified windowWillClose(_:) is public and teardown() is idempotent, so the delegate callback can be invoked directly with a synthetic Notification. The implementation is correct (shared teardown from both close() and the delegate), so risk was low, but a false reason for a coverage gap is worse than the gap: it stops anyone else from closing it.
Closed by me: added closeButtonPathTearsDown, exercising the path a real user actually takes (clicking the close button) and asserting count, policy AND playback all tear down. Note the asymmetry it guards -- close() is the programmatic door, the close button is the common one, and only the former had coverage.
closeButtonPathTearsDown verified by mutation: skipping teardown() fails ALL THREE assertions -- openWindowCount 1 != 0, activationPolicy .regular != .accessory, player.rate 1.0 != 0. Exactly the stranded-Dock-icon-with-invisible-audio failure.
MY OWN PROCESS ERROR, worth recording because it is the exact trap I have been warning implementers about all milestone: my FIRST mutation attempt reported a false negative. I used str.replace() and printed "mutated" UNCONDITIONALLY, without asserting the target string was found. The replace silently matched nothing, the binary was unchanged, the test passed, and for a moment it looked like my new test did not discriminate. A mutation that does not fail is as likely to be a bad mutation as a bad test -- I have told four implementers that this milestone and then walked into it myself. The fix is one line: assert the target exists before writing, and grep the mutated file before running.

Task 5: complete f0c0337, 376 tests, 4 clean runs from a clean build, zero warnings. stopRecording() now calls openEditorIfHuman(for:) after finalisation, reading the initiator stamp from the just-written meta.json (NOT agentSessionID, which is already cleared by that point -- a real trap it navigated), and only for .human builds the composition, computes jump points and shows the window, all inside do/catch that logs and returns normally.
IT FOUND A REAL PRODUCTION BUG THE BRIEF NEVER MENTIONED: EditorWindowController had NO SELF-RETENTION. A window opened by the stop path -- where no caller holds a reference -- would deallocate immediately, because NSWindow.delegate is weak. Fixed with a self-retaining static array mirroring the existing count bookkeeping.
This is the harness-has-properties-production-does-not class INVERTED, and it is the most interesting instance yet. Every test holds a local `let editor = ...` for the duration of the test body, so the controller is retained by the test itself. Production has no such reference. The tests could NEVER have caught this, no matter how well written -- the feature would simply not have worked, and the milestone's automated suite would have stayed green while the window vanished the instant it opened.
Two other deviations, both sound: (1) stopForTesting could not take initiator: as I sketched it, because Recorder's test seam is internal to SnittCapture and unreachable from SnittApp production code -- split into setActiveForTesting(_:) + stopForTesting() with the Recorder built in the test file via @testable import. (2) The three new tests went into EditorWindowControllerTests.swift's existing @Suite(.serialized) rather than RecordingCoordinatorTests.swift, because a SECOND independently-serialised suite touching the same openWindowCount and activation-policy globals races against the first -- confirmed empirically before merging them. That is a subtlety worth keeping: .serialized serialises WITHIN a suite, not across suites.
Both brief-named mutations discriminated correctly.
ALL M4a TASKS COMPLETE: 1, 1b, 2, 2b, 3, 4, 5.

WHOLE-BRANCH REVIEW (opus): SHIP. §9's guarantee confirmed BY MUTATION, not by reading -- cuts, scale-transform, audio mix and marker positions all originate in CompositionBuilder.build, and both consumers read built.keptRanges (MovieExporter.swift:285, RecordingCoordinator.swift:484) rather than recomputing. The two marker mappers are genuinely unified onto TimeRangeMapping.trimmedTime with an agreement test. Basics: 376 passing, 4 clean runs from rm -rf .build, zero warnings, otool clean on both frontends, dependency boundaries intact.
IMPORTANT #1, and the sharpest demonstration yet of why mutation testing is the review instrument: THE PREVIEW NEVER VERIFIES IT RECEIVES THE MIX. The reviewer replaced `item.audioMix = built.audioMix` with a comment and ALL 376 TESTS STILL PASSED. attachesTheGivenComposition checks item.asset identity and renderSize but says nothing about audioMix. A refactor dropping that line silently un-mutes the PREVIEW while export stays muted -- §9's exact divergence in the one direction nothing guards. The code is correct, so reading it reveals nothing; only breaking it does. Export half is well covered (loud 67677 vs quiet 7273, a 9x margin, not fragile).
IMPORTANT #2: two size-ladder tests flake ~1-in-5 (MovieExporterTests 381, 477), failing at 621311 vs a 590953 bound and 340502 vs 311028. Pre-existing M3d, not M4a's doing, but they cost a trustworthy clean signal on the suite that now carries §9's export proof.
MINOR: humanStopOpensEditor leaks a real window for the rest of the run; the suite absorbs it because other tests are before-relative EXCEPT closeButtonPathTearsDown, which asserts an absolute openWindowCount == 0 and survives only because serialised suites run in SOURCE ORDER. Reordering the file breaks it. That is my test, added this milestone -- I made it absolute when everything around it was relative.
MINOR: gain unvalidated end to end; a negative or >1 gain from a hand-edited edit.json is undefined rather than clamped.
MINOR, no code change: stopRecording() now awaits the whole composition build before returning .stopped, so a human hotkey stop reports completion later than before. Harmless at 1.0 scale on short clips; worth a comment for M4b.
NO FINDING on lifetime or Sendable: every close path funnels through the idempotent teardown() which removes from Self.open, with no failure path between show() and teardown that can strand a retention; and BuiltComposition's @unchecked Sendable still holds with AVAudioMix aboard, because the mix is built inside build, never re-referenced, and the MainActor hop is a hand-off rather than sharing.
RULING: I OVERRULED the reviewer's suggestion to defer the gain clamp to M4b. This milestone's own ledger records the same "latent, nothing writes it yet" reasoning being wrong TWICE -- a deferred hazard returned as a SIGSEGV that silently truncated suite runs, and a deferred track-naming bug would have made muting system audio a silent no-op. Clamping now costs three lines. Cost if wrong: three lines of defensive code in a mix builder.
Fix wave dispatched (sonnet, a9bdb9b3) covering all five, with the size-ladder thresholds to be derived from the MEASURED NO-OP RATIO rather than widened by guess.

FIX WAVE complete, 764bb61, all five findings. 378 tests (376 + 2 gain-clamp), 8/8 clean runs post-fix, zero warnings from a clean build.
 1. attachesTheGivenComposition now checks the attached AVAudioMix. Note the echo of the earlier finding: identity-on-readback is NOT achievable for audioMix either -- AVFoundation snapshots it exactly as it does videoComposition -- so the test checks CONTENT, with a muted-track fixture so the mix is non-nil rather than passing vacuously on nil === nil.
 2. Both flaky thresholds RE-DERIVED BY MEASUREMENT rather than widened by guess, which was the explicit ask: scaleReductionActuallyShrinksTheFile -> 0.85 (true-positive 0.30-0.62, no-op 0.947); fileLengthLimitAloneShrinksTheFile -> 0.90 (true-positive 0.30-0.79, no-op 0.97-1.0). Each measured over ~30 runs.
 3. closeAllForTesting() seam added; humanStopOpensEditor cleans up after itself; closeButtonPathTearsDown now asserts relative to a captured `before` instead of an absolute 0, so it no longer depends on source order.
 4. Gain clamped to 0...1 in the mix builder, with negative and >1 tests. TrackState and EDL validation untouched, so a strange sidecar still opens.
 5. Latency tradeoff documented at the await site for M4b.
RESIDUAL, honestly flagged by the implementer rather than buried: fileLengthLimitAloneShrinksTheFile's 0.90 threshold sits between a 0.97-1.0 no-op floor and a 0.79 true-positive ceiling -- 0.07 of headroom on the no-op side against 0.11 on the other. Near the midpoint of the measured window, so it is the right choice available, but it is the tighter of the two and this same test has flaked at a boundary before. Carrying to M4b: if it flakes again the answer is a different observable, not another threshold nudge -- three threshold adjustments on one assertion would mean the assertion is wrong.

FIX WAVE RE-REVIEW: all five ADDRESSED, READY TO PR. Verified by execution in a pinned worktree.
 1. All three parts of the chain checked: identity-on-readback is genuinely unwinnable (built.audioMix === item.audioMix is false even with correct volumes attached -- AVAudioMix snapshots exactly as videoComposition does); the fixture yields a non-nil mix so the assertion is not vacuous; and commenting out the assignment fails the test with item.audioMix -> nil.
 2. Both no-op ratios independently re-measured by mutation rather than taken from the report: sizeLadder -> [1.0] gives 0.986-1.032 over 5 runs (report said 0.947 -- reviewer ran higher, same conclusion); removing fileLengthLimit gives 0.994-1.045 (report said 0.97-1.0, consistent). Both thresholds correctly anchored BELOW the no-op floor and ABOVE the true-positive ceiling. 6 clean SnittExportTests runs, zero flakes.
 3. Reordered the two tests in the file and reran -- all 8 still pass. That is the property that was actually broken, and reading the assertion would not have proved it.
 4. Clamp works for negative and >1. TrackState and EDL validation confirmed untouched by grep -- a strange sidecar still opens.
 5. The latency comment is present and says something useful: it names the correct future fix (race the build, do not try to speed it up).
RESIDUAL FOUND BY THE CHECK I ASKED FOR, and it was real: NaN gain was NOT caught. min(max(Float(Double.nan), 0), 1) evaluates to NaN because every comparison with NaN is false, and setVolume accepts it silently -- verified end to end, getVolumeRamp reads back NaN with ok=true. No crash, no error, just undefined playback volume that nothing reports.
The reviewer graded it non-blocking. I OVERRULED that and fixed it: one line (isFinite ? clamp : 1.0), and it is silent corruption in an audio path. This milestone's record has "narrow, no crash, defer it" being wrong twice. Verified by mutation -- removing isFinite fails both assertions with volume -> nan.
Note the shape: the ORDINARY clamp catches the cases it was written for (negative, >1) and this one passes straight through it. A guard that handles the cases you thought of is not the same as a guard that handles the domain.
M4A COMPLETE: 17 commits, 379 tests, 3/3 clean after the NaN fix.
